# This file intentionally delegates to core/management/commands/backup_db.py
# The canonical backup_db command lives in core to avoid duplication.
# Django will use the core app's version since core is listed first in INSTALLED_APPS.
